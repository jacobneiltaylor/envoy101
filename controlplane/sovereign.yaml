sources:
  - type: file
    scope: routes
    config:
      spec:
        protocol: file
        serialization: yaml
        path: /opt/sovereign/routes.yaml

templates:
  default:
    routes: python://routes.py
